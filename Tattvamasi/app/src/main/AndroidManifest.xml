<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayyappasamaaj.tattvamasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.BhajansActivity" />
        <activity android:name=".view.PoojaActivity" />
        <activity android:name=".view.ArticlesActivity" />
        <activity android:name=".view.EventsActivity" />
        <activity android:name=".view.AboutUsActivity" />
        <activity android:name=".view.DonateActivity" />
    </application>

</manifest>